import React from 'react'

const ToastSuccess = (props) => {
    // const { type, position, message, } = props
    return (
        <div>ToastSuccess</div>
    )
}

const ToastInfo = (props) => {
    // const { type, position, message, } = props
    return (
        <div>ToastInfo</div>
    )
}

const ToastWarning = (props) => {
    // const { type, position, message, } = props
    return (
        <div>ToastWarning</div>
    )
}

const ToastDanger = (props) => {
    // const { type, position, message, } = props
    return (
        <div>ToastDanger</div>
    )
}

export { ToastSuccess, ToastInfo, ToastWarning, ToastDanger }